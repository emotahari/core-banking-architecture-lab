<!-- bidi: rtl; code: ltr -->
<div dir="rtl" align="right">

# <bdi dir="ltr">Day 08 Exit Ticket</bdi> — <bdi dir="ltr">Clean Code</bdi> و <bdi dir="ltr">Strategy/Factory</bdi>

بدون مراجعه به درس، حداکثر در پنج دقیقه پاسخ بده.

1. تفاوت <bdi dir="ltr">Refactoring</bdi> با تغییر <bdi dir="ltr">Rule</bdi> دامینی چیست؟
2. چرا وجود <bdi dir="ltr">`switch`</bdi> به‌تنهایی <bdi dir="ltr">Strategy</bdi> را توجیه نمی‌کند؟
3. <bdi dir="ltr">Characterization Test</bdi> چه چیزی را ثابت می‌کند و چه چیزی را ثابت نمی‌کند؟
4. <bdi dir="ltr">`boolean preferredCustomer`</bdi> چه <bdi dir="ltr">Smell</bdi>ی دارد و یک جایگزین معنادار چیست؟
5. <bdi dir="ltr">Registry/Factory</bdi> در این مسئله باید مالک چه تصمیمی باشد و چه منطقی نباید داخل آن قرار گیرد؟
6. یک هزینهٔ واقعی اضافه‌کردن <bdi dir="ltr">Strategy</bdi> را نام ببر.

## معیار قبولی

- هر شش پاسخ باید به مسئلهٔ <bdi dir="ltr">Transfer Fee</bdi> وصل باشند.
- پاسخ تعریف‌محور بدون <bdi dir="ltr">Trade-off</bdi> حداکثر نصف امتیاز می‌گیرد.
- سؤال ۲ و ۶ <bdi dir="ltr">Critical</bdi> هستند؛ <bdi dir="ltr">Pattern worship</bdi> مانع قبولی است.

</div>
