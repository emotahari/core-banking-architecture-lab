# Day 05 Exit Ticket — Spring Modulith Modules

- Timebox: 5 minutes
- Code and lesson: closed

## Questions

1. Why does creating six Spring Modulith modules not imply six Microservices?
2. What is exposed by default from a closed Application Module, and what is internal?
3. What does `allowedDependencies = {}` mean in Spring Modulith 2.1.0?
4. When is a `@NamedInterface` justified, and what is the risk of naming every subpackage as an interface?
5. Why is moving types into a global `common` package a weak way to fix a dependency cycle?

## Rubric — 5 points

- logical versus deployable boundary: 1
- API/internal/allowlist correctness: 2
- named interface and coupling reasoning: 2

Pass: 4/5.
